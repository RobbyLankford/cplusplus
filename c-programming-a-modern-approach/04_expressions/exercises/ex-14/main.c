#include <stdio.h>

int main(void)
{
    int a, b, c, d, e;

    // Part A
    (a * b) - (c * d) + e;

    // Part B
    (a / b) % (c / d);

    // Part C
    (-a) - b + c - (+d);

    // Part D
    a * ((-b) / c) - d;

    return 0;
}