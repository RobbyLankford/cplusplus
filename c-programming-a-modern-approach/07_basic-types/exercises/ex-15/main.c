#include <stdio.h>

typedef char Int8; // char uses 1 btye (8 bits) of memory
typedef short Int16; // short uses 2 bytes (16 bits) of memory
typedef int int32; // int uses 4 bytes (32 bits) of memory

int main(void)
{
    return 0;
}
