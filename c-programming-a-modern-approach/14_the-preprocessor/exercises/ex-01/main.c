#include <stdio.h>

/* Parameterized Macros */

// Cube of x
#define CUBE(x) ((x) * (x) * (x))

// Remainder when n is divided by 4
#define MOD4(n) ((n) % 4)

// 1, if product of x and y is less than 100, 0 otherwise
#define PROD_CHECK(x, y) (((x) * (y)) < 100 ? 1 : 0)

int main(void)
{
    return 0;
}
